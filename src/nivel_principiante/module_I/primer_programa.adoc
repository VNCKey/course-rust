= 👋 Primer Programa en Rust con Cargo
:source-highlighter: rouge
:rouge-style: monokai
:icons: font
:toc: left
:toclevels: 3
:toc-title: Contenido
:sectnums:
:experimental:

== Instalación de Rust

=== Linux / macOS
[source,bash]
----
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source ~/.cargo/env   # recarga PATH
----

=== Windows
1. Instala *Build Tools for Visual Studio* (MSVC).
2. Descarga `rustup-init.exe` desde https://rustup.rs y ejecuta.

=== Verificación
[source,bash]
----
rustc --version
cargo --version
----

== ¿Qué es Cargo?

Cargo es el **sistema de compilación y gestor de paquetes** oficial de Rust:
crea proyectos, compila, descarga dependencias, ejecuta tests, genera documentación y publica crates.

[NOTE]
====
Equivalente a `npm` (JS), `pip` (Python) o `maven` (Java) pero **todo en una sola herramienta**.
====

== Crear y explorar tu primer proyecto

=== Crear el proyecto
[source,bash]
----
cargo new hello_cargo
cd hello_cargo
----

=== Estructura que genera Cargo
[source]
----
hello_cargo/
├── Cargo.toml      # metadatos y dependencias
├── .gitignore      # ya configurado
└── src/
    └── main.rs     # punto de entrada
----

=== Contenido inicial de *main.rs*
[source,rust]
----
fn main() {
    println!("Hello, world!");
}
----

=== *Cargo.toml* (mínimo)
[source,toml]
----
[package]
name = "hello_cargo"
version = "0.1.0"
edition = "2021"

[dependencies]
----

== Compilar y ejecutar

=== Ciclo rápido de desarrollo
[source,bash]
# solo verifica errores (muy rápido)
cargo check

# compila + ejecuta
cargo run

# compila sin ejecutar
cargo build
----

=== Diferencias de perfil
[cols="1,2,2", options="header"]
|===
|Perfil |Comando |Optimización
|Debug |`cargo build` |Ninguna → rápida compilación
|Release |`cargo build --release` |Máxima → binario rápido/pequeño
|===

=== Ubicación de binarios
- **Debug**: `target/debug/hello_cargo`
- **Release**: `target/release/hello_cargo`

== Limpieza y comandos útiles

[source,bash]
----
cargo clean           # borra target/ (libera espacio)
cargo update          # actualiza deps respetando semver
cargo doc --open      # genera y abre documentación
cargo test            # ejecuta tests
----

== Dependencias (mini-guía)

=== Añadir una crate
Opción 1: editar *Cargo.toml*
[source,toml]
----
[dependencies]
rand = "0.8"
----

Opción 2 (Rust ≥ 1.62):
[source,bash]
cargo add rand
----

=== Versionado semántico
- `"1.0"` → caret `^1.0` (compatible 1.x.x)
- `"=1.0.5"` → exacta
- `"*"` → última (no recomendado en prod)

== Flujo de trabajo típico

[source,bash,linenums]
----
cargo new mi_app
cd mi_app
# editar src/main.rs
cargo check              # iteración rápida
cargo run                # prueba
cargo build --release    # versión final
----

== Próximos pasos

Ahora que dominas la cadena *editar → check → run → release*, puedes continuar con:

* Variables y mutabilidad (`let` vs `let mut`)
* Tipos de datos (escalares, compuestos, colecciones)
* Ownership & borrowing
* Control de flujo y funciones

== Recursos

* https://doc.rust-lang.org/cargo/ [Cargo Book]
* https://crates.io [crates.io] – buscar dependencias
* https://doc.rust-lang.org/book/ch01-03-hello-cargo.html [The Rust Book – Hello Cargo]

[quote]
____
🦀 *Recuerda*: Cargo es tu mejor amigo en Rust. Aprende a dominarlo y tu productividad se multiplicará.
____
