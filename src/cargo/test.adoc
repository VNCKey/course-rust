= Tabla de ejemplo avanzado
:icons: font
:source-highlighter: rouge
:toc:

// 1. Definimos anchos de columna (20 %, 30 %, 25 %, 25 %)
// 2. options="header,footer,autowidth"  → header/footer y ajuste automático
// 3. stripes=even                       → filas alternas coloreadas
// 4. frame=topbot grid=all              → bordes superior/inferior + rejilla completa
// 5. #blanco y #gris son colores custom (puedes usar css)

[cols="2,3,2,2", options="header,footer,autowidth", stripes=even, frame=topbot, grid=all]
|===
| *Modo*      | *Comando*                | *Ubicación*        | *Notas*

// Fila normal
| *Debug*
a| `cargo build`            // 'a' = asciidoc (permite bloques dentro)
| `target/debug/`
| icon:check[] Rápida

// Fila con spans (fusionar horizontalmente)
.2+| *Release*              // .2+ = ocupar 2 filas
.2+| `cargo build --release`
.2+| `target/release/`
| icon:clock[] Lenta

// Celda interna con bloques adicionales
| Pasos:
  . Obtener deps
  . Compilar
  . Linkar
| icon:star[] Optimizada

// Footer (opcional)
| *Resumen*
| *Comando depende del modo*
| *Output en target/*
| *Elegir según necesidad*
|===

// Nota general debajo de la tabla
NOTE: Los tiempos son aproximados y dependen del crate.

// CSS rápido (opcional) para colorear header/footer
++++
<style>
table thead { background: #f4f4f4; }
table tfoot { background: #e8e8e8; font-weight: bold; }
</style>
++++
